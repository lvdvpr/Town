<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.town.mapper.UserMapper">
    <insert id="insertUser" parameterType="User">
        insert into users
            (user_id, user_password, user_name, user_email, user_phone, user_zipcode, user_basic_address, user_detail_address)
        values
            (#{id}, #{password}, #{name}, #{email}, #{phone}, #{zipcode}, #{basicAddress}, #{detailAddress})
    </insert>

    <select id="getUserById" parameterType="String" resultType="User">
        select *
        from users
        where user_id = #{value}
    </select>

    <select id="getUserByEmail" parameterType="String" resultType="User">
        select *
        from users
        where user_email = #{value}
    </select>
</mapper>